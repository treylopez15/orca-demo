{
  "name": "orca-slack-rag",
  "private": true,
  "description": "ORCA UI is static assets + FastAPI; npm scripts start uvicorn for convenience.",
  "scripts": {
    "dev": "python3 -m uvicorn main:app --reload --host 127.0.0.1 --port 8000",
    "start": "sh -c 'python3 -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}'"
  }
}
